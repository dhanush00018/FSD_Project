# Backend configuration
# Update MONGO_URI to point to your MongoDB instance (local, Docker, or Atlas)
MONGO_URI=mongodb://localhost:27017/mean-job-portal

# Replace with a strong random string
JWT_SECRET=change_me_super_secret

# Optional: override backend port (defaults to 3000)
# PORT=3000
